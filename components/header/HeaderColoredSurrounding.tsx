import React, { ReactElement, ReactNode } from "react";
import "./headerColoredSurrounding.scss";

/* interface HeaderColoredSurroundingProps {
  children: ReactNode;
} */

const HeaderColoredSurrounding = () =>
  //{}: children,
  //HeaderColoredSurroundingProps
  {
    return <div className="surrounding"></div>;
  };

export default HeaderColoredSurrounding;
